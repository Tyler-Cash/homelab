apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: normal-priority
value: 100
preemptionPolicy: PreemptLowerPriority
globalDefault: true
description: "Normal priority for all pods."
