app-template:
  image:
    repository: ghcr.io/bjw-s/calibre-web
    tag: 0.6.21@sha256:bd20cf7d8f393adae6adc6f8e60ac2b986d7c2e5b92b863b033b8308d9095656

  enableServiceLinks: false

  env:
    CACHE_DIR: /cache

  service:
    main:
      ports:
        http:
          port: 8083

  ingress:
    main:
      enabled: true
      hosts:
        - host: &host calibre.k8s.tylercash.dev
          paths:
            - path: /
              pathType: Prefix
              service:
                port: 8083
      tls:
        - secretName: calibre-letsencrypt-certificate
          hosts:
            - *host

  persistence:
    books:
      enabled: true
      type: pvc
      existingClaim: calibre-books
    cache:
      enabled: true
      type: emptyDir
      mountPath: /cache
      sizeLimit: 100Mi

  resources:
    requests:
      cpu: 15m
      memory: 249M
    limits:
      memory: 299M
