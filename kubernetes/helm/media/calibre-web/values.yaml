app-template:
  image:
    repository: ghcr.io/bjw-s/calibre-web
    tag: 0.6.20@sha256:d6adbb175e1939a1d14ad27b729699f1a3c892d47342c41adba89f301bd054ed

  enableServiceLinks: false

  env:
    CACHE_DIR: /cache

  service:
    main:
      ports:
        http:
          port: 8083

  ingress:
    main:
      enabled: true
      hosts:
        - host: &host calibre.k8s.tylercash.dev
          paths:
            - path: /
              pathType: Prefix
              service:
                port: 8083
      tls:
        - secretName: calibre-letsencrypt-certificate
          hosts:
            - *host

  persistence:
    books:
      enabled: true
      type: pvc
      existingClaim: calibre-books
    cache:
      enabled: true
      type: emptyDir
      mountPath: /cache
      sizeLimit: 100Mi

  resources:
    requests:
      cpu: 15m
      memory: 249M
    limits:
      memory: 299M
