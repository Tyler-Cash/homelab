
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: coredns-blacklist
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
  storageClassName: ceph-filesystem
