apiVersion: v1
kind: Service
metadata:
  labels:
    app: rook-ceph-mgr
    ceph_daemon_id: a
    rook_cluster: storage
  name: rook-ceph-mgr-dashboard
  namespace: storage
  ownerReferences:
    - apiVersion: ceph.rook.io/v1
      blockOwnerDeletion: true
      controller: true
      kind: CephCluster
      name: storage
      uid: 2340017e-681d-4436-8b4f-9aef4c4b0d19
spec:
  ports:
    - name: http-dashboard
      port: 7000
      protocol: TCP
      targetPort: 7000
  selector:
    app: rook-ceph-mgr
    ceph_daemon_id: a
    rook_cluster: storage
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}