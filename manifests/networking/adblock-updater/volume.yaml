apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: coredns-blacklist
spec:
  storageClassName: "ceph-filesystem"
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi