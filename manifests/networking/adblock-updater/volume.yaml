apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: coredns-blacklist-pvc
spec:
  storageClassName: "ceph-filesystem"
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi