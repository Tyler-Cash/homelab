external-dns:
  serviceMonitor:
    enabled: true
  provider: cloudflare
  txtPrefix: prefix.
  logLevel: debug
  env:
  - name: CF_ZONE_API_TOKEN
    valueFrom:
      secretKeyRef:
        name: cloudflare-secrets
        key: zone-id
  - name: CF_API_KEY
    valueFrom:
      secretKeyRef:
        name: cloudflare-secrets
        key: api-token