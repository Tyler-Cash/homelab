apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: generate-yearly-snapshots
spec:
  generateExistingOnPolicyUpdate: true
  rules:
    - name: generate-yearly-snapshots
      match:
        any:
          - resources:
              kinds:
                - Namespace
      generate:
        namespace: "{{request.object.metadata.name}}"
        synchronize: true
        apiVersion: snapscheduler.backube/v1
        kind: SnapshotSchedule
        name: yearly-snapshot-schedule
        data:
          apiVersion: snapscheduler.backube/v1
          kind: SnapshotSchedule
          metadata:
            name: yearly-snapshot-schedule
          spec:
            disabled: false
            claimSelector:
              matchLabels:
                snapshot.home.arpa/enabled: "true"
            retention:
              maxCount: 3
            schedule: "@yearly"
            snapshotTemplate:
              snapshotClassName: ceph-block-snapshot
